# Pseudo-code example for your integration
if message.text == "Show Master List" and message.user == U0AHVBJD4EQ:
    # Read master_tasks.csv
    # Send content to user via DM (not channel)
else:
    # Reject request or remind user they do not have clearance